# Optional environment file (DO NOT put real secrets in version control).
# This project currently uses Firebase platform config files for most settings.
#
# If you add dotenv support, you can store non-sensitive toggles here and keep
# real secrets in `.env` (which is ignored by `.gitignore`).
#
# EXAMPLE_FLAG=true
